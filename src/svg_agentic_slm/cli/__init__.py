"""Command-line interface for svg-agentic-slm.

The CLI is intentionally thin: it parses inputs, loads configs,
calls the appropriate service functions, and prints results.
No business logic lives here.
"""
